# Native Windows release-build dependencies.
# Ollama itself and model weights are user-managed local services; they are not
# embedded in the installer.
aiohttp>=3.9
psutil>=5.9
cryptography>=42.0
Pillow>=10.0
numpy>=2.0
pytesseract>=0.3
pyautogui>=0.9
pygetwindow>=0.0.9
playsound3>=3.0
pvporcupine>=3.0
pyinstaller>=6.0
