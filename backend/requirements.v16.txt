# Optional V16 capability dependencies (core needs only aiohttp + psutil).
# Each capability self-degrades to UNAVAILABLE/ERROR when its deps are missing.
Pillow>=10.0            # screenshots / OCR / vision
pytesseract>=0.3.13     # OCR
nvidia-ml-py>=12.560.30 # GPU telemetry via pynvml
faster-whisper>=1.1     # backend STT (live voice)
sounddevice>=0.5        # backend TTS/STT audio I/O
soundfile>=0.12
pyautogui>=0.9.54
pygetwindow>=0.0.9       # GUI automation (Windows)
numpy>=2.0
pytest>=8.0

# Microsoft Neural TTS
edge-tts>=7.0.0
playsound3>=3.0.0
